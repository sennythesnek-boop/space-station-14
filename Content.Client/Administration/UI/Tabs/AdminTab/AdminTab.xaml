<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:at="clr-namespace:Content.Client.Administration.UI.Tabs.AdminTab"
    Margin="4"
    MinSize="50 50">
    <BoxContainer Orientation="Vertical">
        <GridContainer Columns="3">
            <cc:CommandButton Command="banpanel" Text="{Loc admin-player-actions-window-ban}" />
            <cc:CommandButton Command="aghost" Text="{Loc admin-player-actions-window-admin-ghost}" />
            <cc:CommandButton Command="permissions" Text="{Loc admin-player-actions-window-permissions}" />
            <cc:CommandButton Command="announceui" Text="{Loc admin-player-actions-window-announce}"/>
            <cc:UICommandButton Command="callshuttle" Text="{Loc admin-player-actions-window-shuttle}" WindowType="{x:Type at:AdminShuttleWindow}"/>
            <cc:CommandButton Command="adminlogs" Text="{Loc admin-player-actions-window-admin-logs}"/>
            <cc:CommandButton Command="faxui" Text="{Loc admin-player-actions-window-admin-fax}"/>
            <cc:CommandButton Command="achatwindow" Text="{Loc admin-player-actions-window-admin-chat}"/>
            <cc:CommandButton Command="roletimereqs" Text="{Loc admin-player-actions-window-role-time-reqs}"/>
            <cc:CommandButton Command="votingconfig" Text="{Loc admin-player-actions-window-voting-config}"/>
            <cc:CommandButton Command="combatstats" Text="{Loc admin-player-actions-window-combat-stats}"/>
            <cc:CommandButton Command="newlifeconfig" Text="{Loc admin-player-actions-window-new-life-config}"/>
            <cc:CommandButton Command="ttsconfig" Text="{Loc admin-player-actions-window-tts-config}"/>
            <cc:CommandButton Command="playerrecords" Text="{Loc admin-player-actions-window-player-records}"/>
            <cc:CommandButton Command="migrations" Text="{Loc admin-player-actions-window-migrations}"/>
            <cc:CommandButton Command="serverperf" Text="{Loc admin-player-actions-window-server-perf}"/>
            <cc:CommandButton Command="connquality" Text="{Loc admin-player-actions-window-conn-quality}"/>
            <cc:CommandButton Command="entitycensus" Text="{Loc admin-player-actions-window-entity-census}"/>
        </GridContainer>
    </BoxContainer>
</Control>
